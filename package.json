{
  "name": "ember-sim",
  "private": true,
  "version": "0.3.0",
  "description": "Heavily WIP browser fire fluid sandbox (Stam fluids + combustion). Experimental — not production-ready.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vite": "^8.2.2"
  }
}
